<div id="form-main">
    <div id="form-div">
        <form onsubmit="sendEmail(); reset(); return false" class="form" id="form1">

            <p class="name">
                <input name="name" type="text" class="validate[required,custom[onlyLetter],length[0,100]] feedback-input" placeholder="Name" id="name" />
            </p>

            <p class="email">
                <input name="email" type="text" class="validate[required,custom[email]] feedback-input" id="email" placeholder="Email" />
            </p>

            <p class="text">
                <textarea name="text" class="validate[required,length[6,300]] feedback-input" id="comment" placeholder="Message"></textarea>
            </p>

            <div class="submit">
                <input type="submit" value="Send" id="button-blue" />
                <div class="ease"></div>
            </div>

        </form>
    </div>

    <script>
        const inputs = document.querySelector('form')

        function sendEmail() {
            Email.send({
                Host: "smtp.mailtrap.io",
                Username: "74f997a20a48ae",
                Password: "e44574794418ae",
                To: 'contactyoimiyabot@gmail.com',
                From: inputs.elements["email"].value,
                Subject: "New Contact Form",
                Body: inputs.elements["text"].value
            }).then(
                message => swal({
                    title: "Email enviado",
                    text: "Pronto nos pondremos en contacto contigo :D",
                    icon: "success",
                })
            );
        }
    </script>