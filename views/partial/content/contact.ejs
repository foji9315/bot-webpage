<div id="form-main">
    <div id="form-div">
        <form class="form" id="form1">

            <p class="name">
                <input name="name" type="text"
                    class="validate[required,custom[onlyLetter],length[0,100]] feedback-input" placeholder="Name"
                    id="name" />
            </p>

            <p class="email">
                <input name="email" type="text" class="validate[required,custom[email]] feedback-input" id="email"
                    placeholder="Email" />
            </p>

            <p class="text">
                <textarea name="text" class="validate[required,length[6,300]] feedback-input" id="comment"
                    placeholder="Message"></textarea>
            </p>

            <div class="submit">
                <input onclick="sendEmail()" value="Send" id="button-blue" />
                <div class="ease"></div>
            </div>

        </form>
    </div>

    <script>
        const inputs = document.querySelector('form')

        async function sendEmail() {
            console.log('sending info');
            let data = {
                name: $('#name').val(),
                email: $('#email').val(),
                text: $('#comment').val()
            };
            console.log(data);
            $.ajax({
                method: "POST",
                url: "./emails",
                contentType: "application/json; charset=utf-8",
                processData: false,
                data: JSON.stringify(data)
            })
                .done(function (msg) {
                    swal({
                        title: "Email enviado",
                        text: "Pronto nos pondremos en contacto contigo :D",
                        icon: "success",
                    });
                    console.log(response)
                });

            reset();
            return false;
        }
    </script>